<h1>ユーザー一覧</h1>

<% @users.each do |user| %>
  <div>
    <p><%= image_tag user.get_profile_image(100,100) %></p>
    <p><%= user.name %></p>
    <p><%= link_to "Show", user_path(user) %></p>
  </div>
<% end %>
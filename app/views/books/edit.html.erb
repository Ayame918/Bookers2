<%# Book編集 %>
<%= form_with model: @book do |f| %>
  <label for="inputTitle">Title</label>
  <%= f.text_field :title, autofocus: true, id: "inputTitle" %>

  <label for="inputOpinion">Opinion</label>
  <%= f.text_area :body, id: "inputOpinion" %>

  <%= f.submit "Update Book" %>
<% end %>